Legacy API handlers were moved to ../legacy-api/ to prevent route collisions with Next app router handlers under app/api/.

Do not restore these files into frontend/api/ unless you intentionally want Pages API routing to take precedence again.

import { PageShell } from "@/components/page-shell";

export default function HomePage() {
  return (
    <PageShell
      title="Autonomous strategy squads for the Skills Arena."
      description="The new Next.js app is live. This placeholder home page anchors the six-route product shell while preserved API endpoints continue powering Xyndicate's live decision engine."
    />
  );
}

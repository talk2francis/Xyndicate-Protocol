import { PageShell } from "@/components/page-shell";

export default function Page() {
  return (
    <PageShell
      title="Proofs"
      description="Proofs route placeholder for X Layer transaction history, vault activity, and on-chain verification artifacts."
    />
  );
}

import { PageShell } from "@/components/page-shell";

export default function Page() {
  return (
    <PageShell
      title="Docs"
      description="Docs route placeholder for protocol docs, API references, MCP setup, and judge-facing walkthrough content."
    />
  );
}
